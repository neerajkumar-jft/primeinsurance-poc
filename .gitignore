.DS_Store
*.pyc
__pycache__/
.ipynb_checkpoints/
*.egg-info/
.sfdx/
.databricks/

# Local-only Genie space context — not synced to git.
# The genie/ directory holds the portable definition used to bootstrap a
# Genie space in any workspace via the UI, but is kept out of the repo per
# team decision.
genie/
